name: mcq_app
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  get: ^4.7.3
  go_router: ^18.0.0
  url_launcher: ^6.3.2
  intl: ^0.20.2

  # --- API layer -------------------------------------------------------
  dio: ^5.9.2                      # interceptors are how 401/403/409/422 are handled once
  flutter_secure_storage: ^9.2.4   # the token lives in the keychain, never in prefs
  s s: ^2.5.5       # non-secret state: locale, device name, read cache, queue
  uuid: ^4.5.3                     # client_action_uuid — the idempotency key

  # --- Field capture ---------------------------------------------------
  image_picker: ^1.2.3
  flutter_image_compress: ^2.5.0   # target under 1 MB before uploading over bazaar data
  path_provider: ^2.1.5            # a scratch directory for the compressed copy
  geolocator: ^14.0.2
  device_info_plus: ^12.4.0        # prefill device_name with the handset model
  connectivity_plus: ^7.0.0        # drives the offline queue and the stale-cache banner
  flutter_local_notifications: ^19.5.0  # visit reminders from next_visit_date (no server push)
  timezone: ^0.10.1

  # --- The map, the share sheet and the witness signature --------------
  flutter_map: ^8.2.2              # OpenStreetMap tiles — no API key to lose
  latlong2: ^0.9.1                 # the LatLng flutter_map speaks
  share_plus: ^12.0.0              # sending a payment link over WhatsApp
  signature: ^6.4.0                # a witness signature is a much stronger record

  # --- Design layer ----------------------------------------------------
  # The chart library. Bars for "my work" by action type, a donut for the
  # share each action takes, a line for the month. Rendered locally — no
  # tiles, no key, nothing to fetch on a bazaar connection.
  fl_chart: ^1.1.1
  # Staggered list entry and the small state transitions, declared rather
  # than hand-rolled with an AnimationController per widget.
  flutter_animate: ^4.5.2
  # The skeleton sweep. A pulse says "busy"; a sweep says "content is
  # coming and this is its shape".
  shimmer: ^3.0.0
  # Empty-state illustrations. The JSON ships in the bundle — nothing is
  # fetched, so an empty state draws with no signal at all.
  lottie: ^3.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    # The empty-state animations. Bundled, never fetched: an empty state
    # is most often what an officer sees when the signal is worst.
    - assets/lottie/

  # The faces ship in the bundle rather than being fetched at runtime.
  #
  # `google_fonts` downloads on first use and falls back to the platform
  # default until it lands — so an officer opening the app for the first
  # time in a bazaar, which is exactly where these handsets are first
  # switched on, would read a government revenue app set in whatever
  # Android happened to have. 1.3 MB in the APK is the right trade for a
  # font that is always there.
  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700
        - asset: assets/fonts/PlusJakartaSans-ExtraBold.ttf
          weight: 800
    # Nastaliq ships as one variable file. The script does not want heavy
    # weights anyway — hierarchy in Urdu is carried by size, not by weight.
    - family: NotoNastaliqUrdu
      fonts:
        - asset: assets/fonts/NotoNastaliqUrdu-Variable.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
